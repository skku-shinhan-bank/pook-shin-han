class IssuePredictor:
  def __init__(self):
    # loaded_model = BERTClassifier(bertmodel,  dr_rate=0.5).to(device)
    # loaded_model.load_state_dict(torch.load('/content/drive/MyDrive/산학/model_state.pt'))
    pass
  
  def predict(self, review):

    return 0